// pages/index.tsx or in any component
import React from 'react';
import svgs from '../../../src/svgs';

const HomePage: React.FC = () => {
    // Use svgs here as needed
    return (
        <div>
            {/* Render your SVGs */}
        </div>
    );
};

export default HomePage;
